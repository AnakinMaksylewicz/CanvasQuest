this folder contains data and API contracts used to keep the CanvasQuest UI and backend consistent.

here, we put:
- Normalized data shapes returned by the backend (Assignment objects, for instance)
- API endpoint request/response examples
- any notes on required/optional fields

assignment-contract.md:
- defines normalized Assignment format used by Demo Mode and Canvas-sourced data
- The backend should return this shape to the UI so components remaind stable
